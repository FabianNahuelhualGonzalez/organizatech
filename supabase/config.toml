# Local/deployment metadata only. Remote hook URLs and secrets are configured
# manually in QA first; this file does not mutate any Supabase project.

[functions.auth-send-email-hook]
verify_jwt = false

[functions.send-welcome-email]
verify_jwt = true

[functions.send-calendar-reminders]
verify_jwt = false

# Deployment metadata only. QA must configure the scheduler bearer secret, the
# distinct Vault-backed RPC capability, and the cron invocation manually.
[functions.process-training-cycle-lifecycle]
verify_jwt = false
